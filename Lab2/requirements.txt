# Система распознавания языка текста «Толмач» — зависимости
# Лабораторная работа № 2, вариант 4

# Веб-интерфейс
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
python-multipart>=0.0.9       # загрузка файла в форме «Свой текст»

# Извлечение текста из HTML.
# Необязательна: без неё используется разборщик из стандартной библиотеки
# (html.parser), но BeautifulSoup устойчивее к незакрытым тегам.
beautifulsoup4>=4.12

# Нейросетевой метод: персептрон реализован на numpy, без фреймворков
numpy>=1.26

# Графики и схемы для отчёта (tools/make_report.py, tools/make_diagrams.py)
matplotlib>=3.8

# Тесты
pytest>=8.0
httpx>=0.27                   # нужен fastapi.testclient в tests/test_web.py
